/**
 * Utility to clear all Zustand persisted stores
 */
/**
 * Clear all persisted Zustand stores
 */
export declare function clearAllStores(): boolean;
/**
 * Clear specific store
 */
export declare function clearStore(storeName: string): boolean;
